apply plugin: 'net.minecraftforge.gradle.forge'

version = modVersion
group = modGroup
archivesBaseName = 'CustomRPG_Objects'

minecraft {
    version = project.forgeVersion
    mappings = project.mcpVersion
    runDir = 'run'
}

dependencies {
    implementation(project(':EclipsisCommon'))
}